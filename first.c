#include <stdio.h>

int main(){
    //main will return an integer - use return type tactically
    int num = 10; //defined a variable - integer is whole no. - variable = named section of memory you can store information in 
    printf("%d\n", num); //format-prints the value of a as an integer - have to tell is that we have a format - %d is a format specifier meaning 'number'
    return 0; //has to have a return value 

}