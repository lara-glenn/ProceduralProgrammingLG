
#include <stdio.h>

int main() {

    int mark = 40;

    //if statement - no ; needed at end of first line
    if (mark >= 40)
    {
        printf ("The mark of %d is a pass\n", mark);
    }
    else
    {
        printf ("The mark of %d is a fail\n", mark);
    }

    return 0;
}